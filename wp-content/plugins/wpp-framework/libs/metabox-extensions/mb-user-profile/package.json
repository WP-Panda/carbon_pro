{
  "scripts": {
    "css": "node-sass --output-style expanded --indent-type tab --indent-width 1 css/user-profile.scss -o css",
    "postcss": "postcss css/user-profile.css --use autoprefixer -o css/user-profile.css --autoprefixer.browsers=\"last 2 versions, ie >= 9\"",
    "watch": "node-sass --output-style expanded --indent-type tab --indent-width 1 --source-map true -w css -r -o css",
    "zip": "git archive -o mb-user-profile.zip --prefix=mb-user-profile/ HEAD",
    "upload": "scp mb-user-profile.zip metabox:/var/www/metabox.io/downloads/",
    "release": "npm run zip && npm run upload",
    "start": "npm run css"
  }
}
